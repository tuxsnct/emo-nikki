mutation AddDiaryToday($text: String!, $uid: String!, $date: date!) {
  insert_diaries(objects: {text: $text, uid: $uid, date: $date}, on_conflict: {constraint: diaries_uid_date_key}) {
    affected_rows
    returning {
      created_at
      date
      id
      text
      uid
    }
  }
}
