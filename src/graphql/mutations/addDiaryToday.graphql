mutation AddDiaryToday($text: String, $uid: String, $date: date = "now") {
  insert_diaries(objects: {text: $text, uid: $uid, date: $date}, on_conflict: {constraint: diaries_date_uid_key}) {
    affected_rows
    returning {
      id
      text
      date
      uid
    }
  }
}
